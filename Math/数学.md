## 数学

### 1.素数筛法

#### 1.埃氏筛法

如下代码实现:

```c++
void solve(int n)
{
    b[0]=b[1]=true;
    for (int i = 2; i*i <= n; i++)
    {
        if(!b[i])
        {
            for(int j=i*i;j<=n;j+=i)//注意是j+=i
            {
                b[j]=true;
            }
        }
    }
}
```

> 这里是假设所有数都是素数(false),先把0 1排除,然后再遍历,如果这个数是素数,他的倍数就不是素数
>
> 从i*i开始遍历,每次加i保证是i的倍数
>
> 如下是关键点:
>
> ![image-20251119131717545](C:\Users\20421\Desktop\LEARN\算法题型小记录.assets\image-20251119131717545.png)



#### 2.欧拉筛法

最基础的欧拉筛需要用到两个数组

```c++
bool isprime[maxn]; //记录是否为素数
int prime[maxn];    //存储素数
int cnt=0;
```

核心思路如下

```cpp
void gprime(int n)
{
    memset(isprime,1,sizeof(isprime));
    isprime[1]=0;
    for(int i=2;i<=n;i++)
    {
        if(isprime[i])
        prime[++cnt]=i;

        for(int j=1;j<=cnt&&i*prime[j]<=n;j++)
        {
            isprime[i*prime[j]]=0;
            if(i%prime[j]==0)
            break;
        }
    }
}
```

一眼看上去和埃氏筛法大差不差, 但欧拉筛法的灵魂就在于

```cpp
if(i%prime[j]==0)
    break;
```

**每一个合数，只被它的“最小质因子”筛掉，且只筛一次**

> 假设我们要筛掉 **12**。
>
> 12 的质因子有 `2` 和 `3`。
>
> 12 的**最小质因子**是 **2**。所以 12 **理应**在 $i=6$ 的时候，被 $2$ 筛掉（$6 \times 2 = 12$）。
>
> **如果不加 `break`，看看会发生什么：**
>
> 当 $i = 4$ 时：
>
> - 先筛 $4 \times 2 = 8$。
> - 接着筛 $4 \times 3 = 12$。（注意！这里用 $3$ 筛掉了 12）。
> - 此时 `4 % 2 == 0`，但我们**没 Break**，继续跑。
>
> 当 $i = 6$ 时：
>
> - 先筛 $6 \times 2 = 12$。（注意！这里用 $2$ **又**筛了一次 12）。
>
> **结果：12 被筛了两次！**
>
> 第一次是 $4 \times 3$（用 3 筛），第二次是 $6 \times 2$（用 2 筛）。这就是浪费。





### 2.最大公约数与最小公倍数(GCD&LCM)

1. 最大公约数采用**欧几里得算法**,最小公倍数是两数乘积除于最大公约数.

​       也就是说: **两数乘积等于最大公约数乘以最小公倍数**

```c++
//运用了gcd(a,b)=gcd(b,r)的性质,r=a%b
int gcd(int a,int b)
{
    while (b)//辗转相除法
    {
        int temp=a%b;  
        a=b;
        b=temp;
    }
    return a;
}
int lcd(int a,int b,int gcd)
{
    int awa=(a/gcd)*b;//可避免溢出
    return awa;
}
```



### 3.拓展欧几里得算法

用于求 **ax+by=gcd(a,b)** 的解

```c++
ll solve4_1(ll a,ll b,ll *x,ll *y)//注意这里采用指针形式以倒序传递x,y
{
    if (b==0)
    {
        *x=1;
        *y=0;
        return a;
    }
    ll x1,y1;
    ll gcd=solve4_1(b,a%b,&x1,&y1);
    *x=y1;
    *y=x1-(a/b)*y1;

    return gcd;
}
int solve4_2()
{
    long long a,b;
    ll x,y;
    scanf("%lld %lld",&a,&b);
    solve4_1(a,b,&x,&y);//可加入判断逻辑

    ll awa=(x%b+b)%b;//为了使x大于零并且小于b
    printf("%lld\n",awa);
    return 0;
}
```

> 想完全理解还是有点难度



### 4.快速幂

#### 1.基础快速幂

##### 原理:

当求 $a^b$ 时, 我们可以通过 $a$ 自乘自己变成 $a^2$ ,再自乘变成 $a^4$ 

依次类推, 可以表示到 $a^{2^n}$ 

1. 假如要求 $a^{11}$ ，我们可以知道11的二进制是 $(1011)$ 

2. $a^{11} = a^8a^2a^1$  

3. 仔细观察就可以发现, 数字8 2 1对应就是二进制对应位数的对应十进制



**过程:**

- 制作一个 $base$。现在 $base=a$，表示的是，$a^1=a$。待会 $base$ 会变的。

- 制作一个 $ans$，初值 1，准备用来做答案。

- $base$  在每次循环后自乘, 表示 $a^{2n}$ 
- 如果此时b对应的二进制末位为1, 则执行 $ans*=base$



**代码实现:**

```c++
int qpow(int a,int b)
{
    int ans=1,base=a;
    while(b>0)
    {
        if(b&1)
        ans*=base;
        
        base*=base;
        b>>=1;
	}
    return ans;
}
```



##### SP: 关于求余运算

> 求余运算有以下性质:
>
> $$(A+B) \bmod b = (A \bmod b + B \bmod b) \bmod b$$
>
> $$(A \times B) \bmod b = ((A \bmod b) \times (B \bmod b)) \bmod b$$

如果想要快速求出 $a^b$ $mod$  $m$

可在快速幂运算中添加步骤 (基于上述性质二)

```c++
ll qpow(ll a,ll b,ll m)
{
    ll ans=1,base=a;
    while (b>0)
    {
        if(b&1)
        {
            ans*=base;
            ans%=m;
        }
        base*=base;
        base%=m;
        b>>=1;
    }
    return ans;
}
```

这样ans返回的就直接是对应的运算结果, 避免了数字的溢出



#### 2.矩阵快速幂

> 假如给了一个N阶矩阵$$A$$，需要知道 $$A^n$$ 的结果, 应该怎么去计算呢?

实现矩阵快速幂最关键的一步就是了解矩阵乘法

最基础的矩阵乘法, 需要**结构体**构造矩阵.

这种乘法的时间复杂度为$$O(n^3)$$

```c++
ll mod=1e9+7;
struct matrix
{
    ll m[105][105];
    matrix(){
        memset(m,0,sizeof(m));
    }

    static matrix ident() //单位矩阵
    {
        matrix res;
        for(int i=1;i<=n;i++)
        res.m[i][i]=1;
        return res;
    }

    matrix operator*(const matrix &other) const //重载运算符定义矩阵乘法
    {
        matrix res;
        for(int i=1;i<=n;i++)
            for(int k=1;k<=n;k++)
            {
                if(m[i][k]==0)
                continue;
                for(int j=1;j<=n;j++)
                {
                    res.m[i][j]=(res.m[i][j]+m[i][k]*other.m[k][j])%mod;
                }
            }
        return res;
    }
};
```

>  `Matrix operator*(const Matrix &other) const` 其实就是一个函数定义
>
> $$\underbrace{\text{Matrix}}_{\text{1. 返回啥}} \quad \underbrace{\text{operator*}}_{\text{2. 函数名叫啥}} \quad ( \underbrace{\text{const Matrix \&other}}_{\text{3. 参数是啥}} ) \quad \underbrace{\text{const}}_{\text{4. 自身承诺}}$$

注意这里的$$i,j,k$$的顺序, 使用 $$i-k-j$$ 的顺序可以大幅提高运算速度 (虽然都是 $$O(n^3)$$)

下面的写法就跟快速幂差不多了

```c++
matrix qpow(matrix base,ll power)
{
    matrix res=matrix::ident();
    while (power>0)
    {
        if(power&1)
        res=res*base;
        base=base*base;
        power>>=1;
    }
    return res;
}
```



### 5.线性基

用于处理一大堆数字的异或问题，也可以处理某些特定问题的情况个数（如灯泡的开关）

核心就是一个基底，这个基底可以表示某些数字

最核心的操作就是**插入操作**

```cpp
struct LiBasis //结构体非必需
{
    ll p[65]; //存储线性基
    LiBasis(){
        memset(p,0,sizeof(p));
    }
    void insert(ll x)
    {
        for(int i=62;i>=0;i--)
        {
            if((x>>i)&1) //x的第i位如果不为0
            {
                if(!p[i]) //如果p的位置为空，插入
                {
                    p[i]=x;
                    break;
                }
                x^=p[i]; //不为空，就把这一位消去，变为0
            }
        }
    }

};
```

而检查数字k是否能被基表示的操作如下

```cpp
bool check(ll k)
{
    for(int i=62;i>=0;i--)
    {
        if((k>>i)&1) //逐位比对
        {
            if(!p[i]) //消掉第i位，但没有第i位的守门员
                return 0; //既然没人能消掉这一位，k永远不可能是0
            k^=p[i];
        }
    }
    return 1; //此时k已经被消为0
}
```

**关于线性基的性质：**

一个元素个数为$n$的线性基，可以表示 $2^n$ 个数字。而0元素应当存在于任意一个线性基中。所以0元素的存在与否，不对线性基真正的元素个数起作用。

比如线性基$\left \{ 0,1,2 \right \}$和$\left \{ 1,2 \right \}$所能表示的数字均为 $2^2$ 个。
